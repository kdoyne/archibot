<!DOCTYPE html>
<html>
<head>
  <link href='http://fonts.googleapis.com/css?family=Lato:100,300,400' rel='stylesheet' type='text/css'>
  <title>ArchibotApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id= "wrapper">
<header><ul>
<li><%= link_to "Home", root_path %></li>
<li><%= link_to("My Profile", user_path(User.find_by(id:session[:user_id]).id)) unless session[:user_id].nil? %></li>
<li><%= link_to("Please Log In","/login") if session[:user_id].nil? %>
<%= link_to("Log Out #{User.find_by(id:session[:user_id]).first_name}", "/sessions", method:"delete") unless session[:user_id].nil? %></li>
</header>

<%=yield%>
</div>
</body>
</html>
